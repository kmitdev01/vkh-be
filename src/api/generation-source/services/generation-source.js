'use strict';

/**
 * generation-source service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::generation-source.generation-source');
