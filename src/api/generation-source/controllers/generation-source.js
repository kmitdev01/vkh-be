'use strict';

/**
 *  generation-source controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::generation-source.generation-source');
