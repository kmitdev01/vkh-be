'use strict';

/**
 * question-key-type service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::question-key-type.question-key-type');
