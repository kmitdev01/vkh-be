'use strict';

/**
 * publishing-organization service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::publishing-organization.publishing-organization');
